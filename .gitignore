/target
**/*.rs.bk
.env
# Ignore SQLite journal/temp files but NOT data.db
*.db-shm
*.db-wal
*.db-journal
.DS_Store
.claude/

# macOS app bundle (local build artifact)
ArtistOilPaints.app/

# App icon (local asset)
aop.png

# Tailwind CSS standalone CLI binary (download locally)
tailwindcss
